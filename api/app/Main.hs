module Main where

import           App

main :: IO ()
main = startApp
    -- args <- getArgs
    -- case args of
        -- ("swagger:update-schema":_) ->
            -- writeSwaggerJSON
            -- startApp
        -- _ -> startApp
